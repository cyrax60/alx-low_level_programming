#include<stdio.h>
/**
 * main - this is the main fucntion
 *
 * Description: will print some string
 *
 * Return: return 0
 */
int main(void)
{
	int a;

	for (a = 0; a <= 9; a++)
	{
		if (a != 9)
		{
			putchar(a + '0');
			putchar(',');
			putchar(' ')0;
		}
	}
	putchar('\');
	return (0);
}
